# Task 01: Project Scaffolding

Set up the foundational structure for the OpenClaw KMP monorepo.

## Goal
Establish a working Gradle build environment with backend, shared, and frontend modules.

## Sub-tasks
- [x] Initialize root `build.gradle.kts` and `settings.gradle.kts`.
- [x] Configure `gradle.properties` with required version catalogs (Kotlin, Compose, Ktor, SQLDelight).
- [x] Create `shared` module for common logic and models.
- [x] Create `backend` module for the Ktor desktop host.
- [x] Create `composeApp` module for the Compose Multiplatform UI.
- [x] Verify that the project syncs and builds (empty modules).

[<- Back to Roadmap](roadmap.md)
